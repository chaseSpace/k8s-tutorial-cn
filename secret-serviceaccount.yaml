# 创建后，可通过 kk describe secret secret-sa-sample 查看token
apiVersion: v1
kind: Secret
metadata:
  name: secret-sa-sample
  namespace: default
  annotations:
    kubernetes.io/service-account.name: sa1 # 关联已存在的sa（若sa不存在，则无法创建secret）
type: kubernetes.io/service-account-token # 自动为secret创建token